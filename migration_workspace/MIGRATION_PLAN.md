# خطة التحول الشامل إلى Firebase (v1.0.0)

## 🎯 الأهداف
- الانتقال الكامل من PostgreSQL إلى Firebase (Firestore/Auth/Storage).
- تحديث شامل لتطبيقات الويب والأندرويد (Capacitor).
- صفر فقدان بيانات (Zero Data Loss).

## 📅 المراحل (تقسيم مهام الوكلاء - Fast Mode)

### المرحلة 1: التجهيز وهيكلة البيانات (الوكيل الحالي)
- [x] إنشاء مساحة عمل التحول.
- [x] استخراج المخطط الحالي (Schema) وتحويله إلى JSON Schema متوافق مع NoSQL. (تم الإنجاز في 2026-01-13)
- [x] إعداد ملفات الإعداد لـ Firebase (Config). (تم الإنجاز في 2026-01-13)

### المرحلة 2: سكربتات النقل (Migration Scripts)
- [x] إنشاء مسودة سكربت نقل المستخدمين (Sample Migration Script).
- [x] إنشاء سكربت نقل البيانات العلائقية (Projects, Workers, Wells). (تم الإنجاز في 2026-01-13)
- [x] إضافة نظام التحقق من سلامة البيانات (Integrity Check). (تم الإنجاز في 2026-01-13)
- [x] إنشاء سكربتات Batch Processing لنقل البيانات الضخمة (Attendance, Expenses). (تم الإنجاز في 2026-01-13)
- [ ] معالجة العلاقات (Foreign Keys) وتحويلها إلى References أو Nested Documents.
- [ ] تشفير البيانات الحساسة أثناء النقل.
- [ ] تشفير البيانات الحساسة أثناء النقل.

### المرحلة 3: تعديل الـ Backend & Frontend
- [x] إنشاء بنية `FirebaseStorage.ts` لتمثيل واجهة `IStorage`. (تم الإنجاز في 2026-01-13)
- [x] استكمال تنفيذ الدوال الأساسية لـ `IStorage` داخل `FirebaseStorage`. (تم الإنجاز في 2026-01-13)
- [ ] استكمال بقية الدوال المالية والتقارير في `FirebaseStorage`.
- [ ] تحديث `server/index.ts` للتبديل بين `DatabaseStorage` و `FirebaseStorage`.
- [ ] تحديث `client/src/lib/queryClient.ts` ليدعم Firebase SDK.

### المرحلة 4: تحديث تطبيق الأندرويد
- [ ] دمج `@capacitor/firebase-messaging` و `@capacitor-community/firebase-analytics`.
- [ ] تحديث نظام الإشعارات (Push Notifications).

## 🛠 التقنيات المستخدمة
- **Database:** Firebase Firestore (Global Multi-region).
- **Auth:** Firebase Authentication (Identity Platform).
- **Storage:** Firebase Cloud Storage.
- **Migration Tooling:** Node.js, Admin SDK.
