# بروتوكول التسليم (Handover Document)

## 👤 حالة الوكيل الحالي
- **الوضع:** فاست مود (Fast Mode) - الوكيل رقم 2.
- **ما تم إنجازه:** شرح آلية المفاتيح وتجهيز البنية التحتية.

## 🔑 دليل تفعيل المفاتيح (Service Account Key)
1. اذهب لـ Firebase Console > Project Settings > Service Accounts.
2. اضغط على **Generate new private key**.
3. قم بتسمية الملف بـ `serviceAccountKey.json`.
4. ارفع الملف إلى المجلد الرئيسي للمشروع (بجانب ملف `package.json`).

## 🔜 المهام المطلوبة من الوكيل التالي (Next Step)
1. **التحقق من المفتاح:** بمجرد وجود الملف، قم بتشغيل سكربت `integrity-check.ts` للتأكد من الاتصال.
2. **بدء النقل:** تشغيل سكربتات النقل بالترتيب الموضح في الخطة.

## ⚠️ تنبيهات هامة
- لا تقم بحذف قاعدة البيانات القديمة إلا بعد صدور تقرير مطابقة (Matching Report).
- تأكد من وجود ملف `serviceAccountKey.json` في مجلد السيرفر (يطلبه المستخدم).
